class CashRegister:
    def __init__(self):

        self.current_order = {

        }

    # def add_product(given_product_name): adds products to the current order
            # create a CashRegister
            #cash_reg = CashRegister()
            # create instance of Inventory
            # take the selected product 
            # check if a dictionary exists inside {current_order} with the same name
            # if it doesn't exist, create a dictionary for it inside current_order
            # if it does exist, increase the quantity prop in that product's dictionary
            # return current_order

    # def apply_discount_coffee
    # def apply_discount_greentea
    # def apply_discount_strawberries

    # def calculate_total: sums value of products in the current order


